/*$(document).ready(function() {
    var g = document.getElementById('nemesis');
    for (var i = 0, len = g.children.length; i < len; i++) {

        (function(index) {
      //    if(g.children[i].getElementsByClassName("button")){


            g.children[i].onclick = function() {
                //alert(index);
                $(this).hide();
                var sum = index+2;
                alert(sum);
                var v = g.children[sum];
                $(v).hide();
            }
        //  }
        })(i);
    }
});*/
